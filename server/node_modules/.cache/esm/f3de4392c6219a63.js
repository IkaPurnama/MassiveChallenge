let createPool;_c62‍.x([["getConnection",()=>getConnection]]);_c62‍.w("mysql2/promise",[["createPool",["createPool"],function(v){createPool=v}]]);

       const getConnection = async () => {
  try{
    const pool = createPool({
      host: 'localhost',
      user: 'root',
      password: '',
      database: 'ndoroarum'
    })

    return pool;
  } catch(err){
    _c62‍.g.console.log(err)
  }
}